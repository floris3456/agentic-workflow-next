# Contracts

This boundary is reserved for versioned schemas, semantic rules, and valid/invalid examples. The repository has no checked-in remote-control transport contract. No contract is accepted merely because a placeholder directory exists.

Each contract addition must state owner, versioning rule, compatibility rule, canonical example, invalid examples, error taxonomy, privacy classification, and tests.
