# Template-maintenance task progress

## Task ID

TEMPLATE-DIRECT-DEVELOPER-001

## Status

Completed

## Task-start template-development SHA

4818f5cb8f39990152300d101b13df3357c0a585

## Review-base template-development SHA

4818f5cb8f39990152300d101b13df3357c0a585

## Public-safe task brief

Change the MCP-ON protocol so the web orchestrator may directly edit `developer` when the same proportional-completion logic makes that the shortest safe route, while preserving delegation/OpenCode for work where local exploration, implementation complexity, or independent developer execution materially improves confidence.

## Current objective

Remove the blanket delegation requirement for bounded `developer` changes without weakening exact remote evidence, concurrency control, review, testing proportionality, task continuity, recovery, or human-only `main` promotion.

## Current position

The protocol now treats direct connected-GitHub mutation and delegated OpenCode implementation as alternative `developer` execution routes selected by proportionality. Direct edits are allowed only when exact paths/edits are already known, the change is bounded and low enough risk, required branch records can be maintained, no local-only tooling or broad exploration is needed, and focused remote checks can prove the outcome more simply. Work that benefits materially from local repository tools/context, interacting implementation, nontrivial generation/tests, uncertainty, higher risk, or independent implementation remains delegated. The repository-owned template-maintenance contract and design records use the same model.

## Source ranges

- `template-development`: `4818f5cb8f39990152300d101b13df3357c0a585..801d5362c240fa612b5c5424ac8ea81f5a5c0bd0` before this dedicated handoff snapshot.
- `web-orchestration`: `301b37f96b7adae708b199278045feb11c493fdb..9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`.
- `developer`: unchanged at `e2700f586fe8ab634053eb514bb9da487e881a21`.
- `main`: unchanged at `6127611113dfdb66f93a0cfd2d355359aa370833`; no promotion performed.

## Observed

- Live source refs matched `source-lock.json` at task start.
- No open `agentic-bridge` control issue existed at task start.
- Permanent Project policy already required the shortest route that proves the outcome and proportional review/scouting, but MCP-ON implementation still prohibited direct `developer` content writes and always proceeded into a delegated developer response contract.
- The template-maintenance Project Source similarly assumed delegated `developer` implementation, while the repository-owned ledger agreement, skill, README, AS-BUILT, and design described source edits as isolated-worktree work.
- The `developer` branch's `AGENTS.md` is a local developer agreement for delegated/local agent work. The new direct route therefore does not change that agent contract; instead, the orchestrator must still honor the branch's normal task, AS-BUILT/deviation, synchronization, and validation requirements when editing directly.
- The Project package now explicitly allows bounded direct `developer` edits under the proportional criteria and requires delegation when local tooling/context or independent implementation adds material value.
- Direct and delegated mutation may not overlap. Direct work counts as the single repository-mutating developer task for concurrency purposes.
- Direct-route continuity uses `Selected developer: none` and records the direct GitHub mutation in active work/route decisions; delegated work continues to use Luna or Sol and the existing six-field handoff/bridge workflow.
- Human exact-SHA approval remains the only route into `main`, and any later `developer` commit still invalidates a prior promotion approval.

## Interpretation

Delegation is an implementation tool, not an authority boundary. The authority and safety boundaries are exact remote state, one mutation route at a time, branch-owned records, proportional evidence, no mutation replay, independent review, and human-only `main` acceptance. When direct GitHub can satisfy those boundaries more simply, forcing OpenCode adds ceremony rather than confidence; when local execution or independent implementation materially helps, OpenCode remains the correct route.

## Attempts

- Attempted to download the exact web handoff archive and execute `web-orchestration-only/validate-package.mjs` locally. The execution environment could not resolve `github.com`, so no archive was downloaded and no local Project-package validator result is claimed.

## Changed approach

Used connected GitHub exact remote compare/readback for the three changed Project files and inspected the unchanged Project-package validator's relevant contract checks directly. Used the push-triggered `template-development` workflow for executable validation of the reconciled ledger/design changes. No package was hand-built when the tracked package-generation route was unavailable.

## Checks

- Exact web compare `301b37f96b7adae708b199278045feb11c493fdb..9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`: linear (`ahead_by=3`, `behind_by=0`) and changes only `developer-instructions.md`, `skill-mcp-on-workflow.md`, and `skill-mcp-on-template-maintenance.md` under the Project package.
- Exact remote readback at `9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92` confirms the permanent proportional direct-edit capability, the direct-versus-delegated bounded implementation loop, and the same route selection for template maintenance.
- The unchanged Project validator still requires the permanent shortest-route/proportional boundaries, exactly one delegated six-field response block and bridge command example, the quick direct-GitHub lookup path, interaction-before-status behavior, canonical template-maintenance continuity substitution, and human exact-SHA promotion boundary. The changed files preserve those required surfaces.
- Exact template-development compare `4818f5cb8f39990152300d101b13df3357c0a585..801d5362c240fa612b5c5424ac8ea81f5a5c0bd0`: linear (`ahead_by=7`, `behind_by=0`) and changes only the task record, source lock, repository-owned maintenance agreement/skill, README, AS-BUILT, and workflow design.
- Push-triggered `Validate template development` run `31814952532` completed successfully for `801d5362c240fa612b5c5424ac8ea81f5a5c0bd0`.
- No Scout, bridge command, or developer route was launched for this bounded protocol edit.
- `developer` and `main` remained unchanged.

## Blockers / required decisions

None for the requested protocol change. The only validation limitation is the unavailable local execution of the standalone Project-package validator; exact remote review found no contract break and no web branch CI exists to provide an additional executable signal.

## Remaining work

None for this working cycle. A deterministic change package was not generated because the tracked local packaging route is unavailable in this execution environment and package transfer was not part of the requested outcome.

## Next action

None. Future MCP-ON tasks may now select a direct `developer` edit or OpenCode delegation using the same proportional-completion logic. No `main` promotion is requested or authorized.

## Relevant durable records

- `web-orchestration-only/chatgpt-project/developer-instructions.md` at `9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`
- `web-orchestration-only/chatgpt-project/skill-mcp-on-workflow.md` at `9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`
- `web-orchestration-only/chatgpt-project/skill-mcp-on-template-maintenance.md` at `9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`
- `web-orchestration-only/validate-package.mjs` at the same web handoff
- `AGENTS.md`
- `.opencode/skills/template-maintenance/SKILL.md`
- `README.md`
- `docs/architecture/AS-BUILT.md`
- `docs/design/template-maintenance-workflow.md`
- `source-lock.json`

## Last handoff commit

None

## Issue mapping

No open `agentic-bridge` control issue existed at task start, and none was created. No bridge issue was needed because this was a bounded direct Project-package plus ledger-maintenance correction.

## Active work

None.

## Pending publication

None. The Project protocol source is pushed through `9d1df0d31c9e8b83d00e469e46dfb4cb375e1f92`; the reconciled ledger implementation is pushed through `801d5362c240fa612b5c5424ac8ea81f5a5c0bd0` before this handoff snapshot.

## Command / Scout / refusal journals

- No Scout, bridge command, or developer route was launched.
- Local archive download/validation failed before reaching GitHub because DNS resolution was unavailable; it produced no repository effect.

## Findings

- Confirmed and fixed: proportionality governed inspection/review but not the choice between direct and delegated `developer` implementation.
- Confirmed and fixed: the repository-owned template-maintenance contract assumed isolated worktrees even when a bounded direct source edit would be simpler and equally provable.
- Preserved: OpenCode remains the preferred route whenever its local context/tools, exploration, testing/generation, or independent implementation materially improves confidence.
- Preserved: exact remote proof, single-mutation concurrency, branch records/checks, recovery/no-replay rules, independent review, and human-only promotion.

## Decisions

- Direct `developer` mutation is an allowed proportional execution route, not a new universal default.
- A direct edit must be exact-known, bounded, low enough risk, remotely provable, and compatible with branch-owned records/checks.
- Switch to delegation whenever direct-route criteria cease to hold; never overlap direct and delegated mutation.
- Keep `main` human-only and unchanged.
