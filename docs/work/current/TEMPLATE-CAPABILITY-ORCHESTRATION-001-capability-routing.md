# Template-maintenance task progress

## Task ID

TEMPLATE-CAPABILITY-ORCHESTRATION-001

## Status

source redesign is complete and remotely reviewed; portable package generation remains pending only on a legitimate networked maintainer execution surface, with no source-lock ordering dependency

## Task-start template-development SHA

e69f8ba7f13d286205605682788c04141d65d31d

## Review-base template-development SHA

e69f8ba7f13d286205605682788c04141d65d31d

## Public-safe task brief

Simplify the reusable web-orchestrator Project package by removing the global
MCP-ON/MCP-OFF architecture, routing work from the human outcome and capabilities
actually available at the point of use, retaining the real
evidence/safety/authority boundaries, and combining the four always-co-triggered
prompt-creation Sources into one prompt skill while preserving the substantive
prompt-craft techniques. Update all Project skills and permanent developer
instructions, plus the package contracts needed to keep the installation
coherent. Do not promote `main`.

## Current objective

The requested source redesign is complete. Preserve its exact reviewed source
range and current design truth; generate its portable package independently when
a legitimate networked maintainer execution surface is available.

## Current position

The web-orchestrator Project package now uses five conditionally routed skills:
`skill-workflow.md`, `skill-recovery.md`, `skill-template-maintenance.md`,
`skill-promotion.md`, and one unified `skill-prompt-creation.md`. Permanent
developer instructions are capability-local and no longer define model-name or
MCP-ON/MCP-OFF operating modes. Eleven superseded skill files were deleted rather
than retained as aliases. Ordinary task-context templates no longer persist a
global mode. Current prompt design and integrated AS-BUILT records describe the
new architecture. The repository-owned local template-maintenance skill no longer
refers to MCP-ON proportional criteria.

`TEMPLATE-SOURCE-LOCK-SIMPLIFY-001` later changed the maintenance provenance
contract so `source-lock.json` is the latest reconciled canonical source snapshot
rather than a package review-base lock. That later decision removes this task's
former predecessor package-ordering dependency without changing this task's exact
reviewed range.

## Source ranges

- `main`: `6127611113dfdb66f93a0cfd2d355359aa370833`
  (unchanged; no promotion authorized).
- `developer`: `980486182c0ed8a213842477b9b1754de360a430`
  (unchanged by this task).
- `web-orchestration`: exact task range
  `c20854fd566601cbff2cad9aab12af195fe1e5f0..f3caf6b61cf76ade806824d1c4485c6993bfb150`.
- `template-development`: task start
  `e69f8ba7f13d286205605682788c04141d65d31d`; durable design/contract state is
  current through `2ac07462ff73461a1e55697f0ddde9094205ce68`
  before the original dedicated task-progress snapshot.

## Observed

- The former Project package contained twelve skill Sources: eight MCP-mode
  Sources, prompt core, and three prompt support Sources. Prompt destination,
  mission, and craft always co-triggered with prompt creation.
- The final Project directory contains exactly five skills plus README and
  developer instructions. All `skill-mcp-*` Sources and the separate prompt
  destination/mission/craft Sources are absent.
- Permanent instructions now keep only role/evidence, capability-local execution,
  proportional completion, authority/safety, completion, and a five-row router.
- Normal workflow owns lookup/research/review, optional hardened read-only
  scouting, direct-versus-delegated implementation, review/correction, and
  conditional finalization.
- Recovery remains separate because its command/issue/publication/Git
  reconciliation and no-replay rules are exceptional and high-cost.
- Promotion remains separate because it is high-stakes and triggered only by
  explicit human approval of one exact reviewed `developer` SHA.
- Template maintenance remains separate because it owns the independent
  `template-development` continuity and deterministic package/provenance system.
- Unified prompt creation preserves fresh-web-orchestrator and direct-OpenCode
  destinations; research, review, implementation/change, reproduce/test,
  continue/recover, and template-maintenance-transfer missions; Observed /
  Interpretation / Requested outcome; source-role boundaries; and all retained
  craft families: context/evidence organization, adaptive decomposition/planning,
  exploration/anchoring control, examples/demonstrations,
  verification/uncertainty, tool/action framing, output/interface shaping, and
  evaluation-driven optimization.
- Prompt craft remains gated by a material failure mode and attention/token/
  rigidity/autonomy cost; applying no extra technique is valid. Private
  chain-of-thought/hidden scratch work is not requested.
- The new ordinary task-context template records only material capability limits
  when relevant; historical task records are intentionally not rewritten merely
  to match current terminology.
- At the original handoff, `source-lock.json` was deliberately frozen at the
  prior `TEMPLATE-TRUST-BOUNDARY-001` package review bases under the then-current
  provenance design. That historical choice was later superseded by the explicit
  source-snapshot simplification task; the repository source snapshot is now
  reconciled independently from package ranges.

## Interpretation

Capabilities are execution constraints, not global modes. The durable split is
now based on conditional semantic ownership: normal workflow, exceptional
recovery, reusable-template maintenance, human-only promotion, and prompt
context transfer. Always-co-triggered prompt concerns belong as sections of one
skill; conditional procedures remain separate so ordinary context stays small.

The later source-snapshot simplification changes only package/ledger ordering,
not this task's reviewed source membership.

## Attempts

- Exact connected-GitHub reads/writes were used for all source and ledger changes.
- At the time of the original work, a local checkout/executable run of
  `web-orchestration-only/validate-package.mjs` and its Node tests could not be
  established because the available local runtime could not resolve/reach GitHub,
  and the `web-orchestration` branch had no execution workflow. That limitation
  was later fixed by `TEMPLATE-CI-REACHABILITY-001`; the historical attempt remains
  accurate.
- Validator source and negative fixtures were therefore reviewed directly from
  the exact remote revision, with semantic checks made reflow-tolerant where
  prose distance—not policy—was the only issue.

## Changed approach

- Replaced the former twelve-Source routed/support architecture with five
  conditionally routed Sources rather than adding compatibility aliases.
- Folded scouting and finalization into normal workflow because both are
  conditional branches of ordinary completion; kept recovery and promotion
  separate because progressive disclosure materially reduces routine context.
- Folded prompt destination, mission, and craft into prompt creation while
  retaining the substantive technique taxonomy rather than summarizing it away.
- Preserved historical task records as history and changed only current
  templates/design/AS-BUILT contracts during the original redesign.
- Under the original provenance contract, preserved the predecessor source-lock/
  package ordering rather than silently moving the lock. The human later
  explicitly replaced that contract with independent current source snapshots in
  `TEMPLATE-SOURCE-LOCK-SIMPLIFY-001`, removing this task's ordering blocker.

## Checks

- Open `agentic-bridge` issue search was empty before source mutation; no Scout,
  bridge developer, or control issue was launched for this direct maintenance
  task.
- Exact web compare
  `c20854fd566601cbff2cad9aab12af195fe1e5f0..f3caf6b61cf76ade806824d1c4485c6993bfb150`
  is linear (`behind_by=0`) and contains only the intended Project package,
  validator/tests, and task-context contract paths: 11 superseded skill removals,
  four new conditionally routed skills, unified prompt creation, developer
  instructions, installation/overview docs, task-context template/README, and
  validator/test rewrites.
- Exact remote Project inventory confirms README + developer instructions + the
  five intended skill files and no superseded skill aliases.
- Exact remote developer-instruction readback confirms capability-local behavior,
  five routed Sources, public persistence, proportional route selection,
  no-replay, and human exact-SHA `main` authority.
- Exact prompt-skill readback confirms destination/mission/evidence behavior and
  all eight retained craft technique families plus the no-op and hidden-reasoning
  boundaries.
- Exact workflow readback confirms conditional finalization retains unchanged
  task-progress blob/current/archive/OID proof and the hardened Scout boundary is
  retained in the unified workflow.
- `web-orchestration-only/validate-package.mjs` statically enforces the exact
  five-Source inventory, capability-local permanent semantics, unified prompt
  contract, hardened Scout/no-replay/template-provenance/promotion boundaries,
  bridge-envelope shapes, public safety, and new task-context schema.
- `template-development` push-triggered validation runs recorded during the task
  succeeded for the integrated design/AS-BUILT work.
- `main` and `developer` were not mutated by this task.
- `TEMPLATE-SOURCE-LOCK-SIMPLIFY-001` later mechanically proved that package
  ranges may be independent from the current source snapshot while retaining
  canonical fetch/ancestry provenance.

## Blockers / required decisions

No design, source-implementation, review, or source-lock ordering decision remains.

Portable package generation still requires a legitimate networked maintainer
execution surface because packages must be produced by the tracked generator from
fresh canonical Git objects rather than hand-built through repository API writes.
That execution-surface limitation is isolated to producing this task's package;
it no longer blocks later tasks or source-snapshot reconciliation.

## Remaining work

When a legitimate networked maintainer execution surface is available, generate
this task's deterministic package from the preserved exact reviewed web range,
validate and review the generated bytes, and perform normal task finalization.
No predecessor package or source-lock reconciliation is a prerequisite.

## Next action

Generate this task's exact reviewed package when a legitimate maintainer execution
surface is available. Later maintenance work does not need to wait for it. Do not
promote `main`.

## Relevant durable records

- `web-orchestration-only/chatgpt-project/developer-instructions.md`
- `web-orchestration-only/chatgpt-project/skill-workflow.md`
- `web-orchestration-only/chatgpt-project/skill-recovery.md`
- `web-orchestration-only/chatgpt-project/skill-template-maintenance.md`
- `web-orchestration-only/chatgpt-project/skill-promotion.md`
- `web-orchestration-only/chatgpt-project/skill-prompt-creation.md`
- `web-orchestration-only/chatgpt-project/README.md`
- `web-orchestration-only/validate-package.mjs`
- `web-orchestration-only/validate-package.test.mjs`
- `web-orchestration-only/task-context/README.md`
- `web-orchestration-only/task-context/TEMPLATE.md`
- `.opencode/skills/template-maintenance/SKILL.md`
- `docs/design/prompt-creation.md`
- `docs/architecture/AS-BUILT.md`
- `source-lock.json`
- `docs/work/current/TEMPLATE-TRUST-BOUNDARY-001-provenance-scout-isolation.md`
- `docs/work/current/TEMPLATE-SOURCE-LOCK-SIMPLIFY-001-current-source-snapshot.md`

## Last handoff commit

Original source handoff remained `f3caf6b61cf76ade806824d1c4485c6993bfb150`; later source-snapshot semantics are tracked separately.
