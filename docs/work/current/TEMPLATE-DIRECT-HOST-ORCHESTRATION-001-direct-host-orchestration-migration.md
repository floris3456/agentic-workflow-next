# Template-maintenance task record

## Task ID

TEMPLATE-DIRECT-HOST-ORCHESTRATION-001

## Public-safe task brief

Migrate `floris3456/agentic-workflow-next` from the copied GitHub-Issue/OpenCode bridge transport to direct authorized host access through Remote Desktop Commander plus native OpenCode.

Remove transport, recovery, host-administration, and package-broker machinery that exists only because the web orchestrator previously could not reach the host directly. Do not replace the old bridge with another RPC/control plane.

Preserve exact remote Git as repository truth, ordinary relevant tests/CI, no automatic replay of ambiguous mutations, public-safe persistence, and human-only exact-SHA `main` acceptance.

## Canonical execution order

This record owns the overall migration order:

1. **Direct Host — substrate.** Establish the independent new-repository worktree/canonical identity and prove direct native OpenCode control, ordinary push/readback/CI, and simple no-replay recovery while the old bridge remains available.
2. **Dual Developer — opt-in.** Implement and prove `dual` as an explicit selectable route. Do not make it the global default yet.
3. **Instruction Minimalism — cutover.** Simplify web/developer/template instructions, make `dual` the default substantive route, establish the web/lead/Spark responsibility split, remove compaction completely, retain the last 5,000 raw chat tokens, and instruct agents to re-read needed durable files.
4. **Representative Dual task.** Run one normal substantive task through the operative Dual route and perform web final outcome/system verification.
5. **Direct Host — retirement.** Remove the obsolete bridge, bridge-only host administration/recovery/protocol/tests/docs, and package Action/request broker while retaining the deterministic package generator/provenance code.
6. **AgentMemory.** Add attributed shared/isolated developer memory as a non-blocking enhancement after the core workflow is stable.

AgentMemory is not a prerequisite for steps 3–5.

## Responsibility boundary

This task owns:

- independent `agentic-workflow-next` worktree/canonical repository identity;
- a compatible direct native OpenCode runtime and normal session control;
- direct structured interaction needed by the selected developer route;
- simple no-replay reconciliation after timeout/disconnect/unknown mutation state;
- removal of GitHub-Issue bridge transport/runtime and bridge-only host administration after the replacement path is proven;
- removal of the package Action/request broker once direct use of the tracked generator is available;
- runtime-facing AS-BUILT/docs/tests needed to describe the retained implementation.

It does not own:

- lead/Spark behavior — `TEMPLATE-DUAL-DEVELOPER-001`;
- route/review/task-record/task-progress/deviation/compaction/lifecycle semantics — `TEMPLATE-INSTRUCTION-MINIMALISM-001`;
- AgentMemory — `TEMPLATE-AGENT-MEMORY-001`.

The previously proposed private host adapter is not part of the plan. Add another abstraction only if actual operation later proves one is needed.

## Step 1 required work — direct-host substrate

Prove the smallest useful direct loop while leaving the old bridge intact:

- operate in the intended `agentic-workflow-next` repository/worktree;
- correct canonical repository/source identity for the new repository;
- start/continue the selected native OpenCode developer session;
- handle structured questions/permissions without broad auto-approval;
- reconcile current local/session/Git/remote state before retrying an ambiguous operation;
- complete one meaningful commit/push and exact GitHub readback;
- run the branch's ordinary relevant tests/CI on the pushed state;
- keep private host/session/credential data out of Git.

Do not build a special verifier framework for this proof. Use direct observation, ordinary checks, and exact remote readback.

There is no additional server-side `main`-protection prerequisite in this task. Preserve the existing human exact-SHA promotion authority and normal branch safeguards without inventing a new migration gate.

## Step 5 required work — retirement

After the instruction cutover and representative Dual task prove the bridge is unnecessary, remove obsolete machinery including:

- GitHub Issues as an OpenCode command/request bus;
- bridge UUID/sequence/mailbox/projection protocol;
- GitHub App bridge daemon and bridge-specific recovery/state;
- bridge-only Workspace host-administration operations;
- bridge-only scripts/tests/docs/validators;
- package-generation Action/request broker.

Retain:

- Remote Desktop Commander/native OpenCode direct execution;
- exact remote Git and ordinary push-triggered CI;
- `dual`, `small`, and `heavy` routes defined by the current instruction/developer records;
- tracked deterministic change-package generator/provenance code when packaging is actually requested;
- human-only exact-SHA `main` promotion;
- simple no-replay recovery and public-safe persistence.

## Recovery invariant

After any operation with an unknown effect:

1. do not automatically repeat it;
2. inspect the existing process/session when relevant plus local Git and exact remote Git state;
3. continue/review the existing effect if it is active or completed;
4. retry/replace only when evidence shows the original effect did not occur or cannot continue.

Do not recreate bridge-style sequence ledgers or generalized recovery state machines.

## Verification philosophy

Prefer the smallest evidence that proves the outcome:

- direct inspection/readback;
- normal implementation tests;
- ordinary branch CI where useful;
- exact remote Git comparison.

Do not add architecture-specific verifier frameworks unless a real repeated failure later demonstrates the need.

## Acceptance criteria

Step 1 is complete when the direct host/OpenCode path can safely execute, recover, push, and be reviewed without using the bridge as transport.

Step 5 is complete when normal work no longer depends on bridge/broker runtime, obsolete machinery is removed, retained runtime/docs/AS-BUILT accurately describe the actual direct system, and ordinary relevant checks pass.

No `main` promotion is part of this task without separate human approval.

## Relevant durable records

- `docs/work/current/TEMPLATE-DUAL-DEVELOPER-001-dual-developer-architecture.md`
- `docs/work/current/TEMPLATE-INSTRUCTION-MINIMALISM-001-instruction-system-redesign.md`
- `docs/work/current/TEMPLATE-AGENT-MEMORY-001-agent-memory.md`
- `source-lock.json`
- applicable architecture/AS-BUILT/deviation records on affected branches
