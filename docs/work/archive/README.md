# Archived template-maintenance tasks

Files here are immutable, public-safe, non-authoritative benchmark history. Each
must be the exact reviewed blob moved from `current/` at the same basename. Never
edit, replace, overwrite, or reuse an archived task identity.
